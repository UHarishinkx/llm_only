# Essential dependencies for ARGO Oceanographic Dashboard
streamlit>=1.28.0
pandas>=2.0.0
numpy>=1.24.0
chromadb>=0.4.0
groq>=0.4.0
duckdb>=0.9.0
plotly>=5.15.0
folium>=0.14.0
streamlit-folium>=0.13.0
matplotlib>=3.7.0
seaborn>=0.12.0
pyarrow>=12.0.0
python-dotenv>=1.0.0
requests>=2.31.0

# Optional dependencies (commented out - install only if needed)
# sentence-transformers>=2.2.0  # Only for advanced embeddings (not required)
# huggingface-hub>=0.16.0       # Only if using sentence-transformers
# fastapi>=0.104.1              # Only for API interface
# uvicorn[standard]>=0.24.0     # Only for API interface
